#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../lib/l42'

output =
  L42::Monad
  .functional_input_or_args(L42::Interfaces::TxSend, ARGV.first, use_params: ARGV.drop(1))
output&.each do |line|
  system(line)
end
# SPDX-License-Identifier: Apache-2.0
